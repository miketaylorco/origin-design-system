name: Sync tokens from Figma

# NOTE: This workflow is currently disabled. Token sync requires the Figma
# Console MCP Desktop Bridge plugin running in the Figma desktop app, which
# is not available in CI. Run the /sync-tokens skill from Claude Code instead.
#
# A future approach could use a headless token extraction mechanism
# (e.g. Figma REST API with an Enterprise token) if that becomes available.

on:
  workflow_dispatch: # manual trigger only — does nothing until re-enabled
